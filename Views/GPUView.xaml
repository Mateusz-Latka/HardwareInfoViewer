<UserControl x:Class="HardwareInfoViewer.Views.GPUView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf">
    <StackPanel Margin="10" HorizontalAlignment="Stretch">
        <StackPanel Margin="0,10" Background="#1E1E1E">
            <TextBlock Text="GPU Usage %" FontSize="18" FontWeight="Bold" Foreground="#00FF88" Margin="0,0,0,5"/>
            <lvc:CartesianChart x:Name="GpuChart1" Height="200"/>
        </StackPanel>

        <StackPanel Margin="0,10" Background="#1E1E1E">
            <TextBlock Text="GPU Temperature °C" FontSize="18" FontWeight="Bold" Foreground="#00FF88" Margin="0,0,0,5"/>
            <lvc:CartesianChart x:Name="GpuChart2" Height="200"/>
        </StackPanel>

        <StackPanel Margin="0,10" Background="#1E1E1E">
            <TextBlock Text="GPU Fan Speed (RPM)" FontSize="18" FontWeight="Bold" Foreground="#00FF88" Margin="0,0,0,5"/>
            <lvc:CartesianChart x:Name="GpuChart3" Height="200"/>
        </StackPanel>
    </StackPanel>
</UserControl>